box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.2"
        theme: minimo
        flags: --buildDrafts=true
deploy:
  steps:
    - uetchy/gh-pages:
        token: $GIT_TOKEN
        repo: mhartington/mhartington.github.io
        path: public
        domain: mhartington.io
