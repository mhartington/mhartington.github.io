box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.15"
        theme: hugo-minimalist-theme
        flags: --buildDrafts=true
deploy:
  steps:
    - uetchy/gh-pages:  
        token: $GIT_TOKEN
        path: public
        domain: mhartington.io
